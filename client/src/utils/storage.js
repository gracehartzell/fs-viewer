const localStorage = Window.localStorage;
/**
 *
 *
 * @param {*} folderPath
 * @param {*} folderContents
 */

const Storage = (folderPath, folderContents) => {
  console.warn("Existence in Storage?", localStorage);
};

export default Storage;
